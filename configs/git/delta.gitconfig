# git-delta. Linked to ~/.config/git/delta.gitconfig only when delta is installed.
[core]
	pager = delta
[interactive]
	diffFilter = delta --color-only
[delta]
	navigate = true
# Theme engine colors; the file exists only when theming is set up.
[include]
	path = ~/.config/tinted-theming/tinty/delta.gitconfig
